services:
  - type: web
    name: ensolvers-backend
    env: java
    buildCommand: ./gradlew clean build -x test
    startCommand: java -jar build/libs/backend-0.0.1-SNAPSHOT.jar
    autoDeploy: true
